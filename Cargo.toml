[package]
name = "cuda-attention"
version = "0.1.0"
edition = "2021"
description = "Attention system — saliency, habituation, change detection, focus modes"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Lucineer/cuda-attention"
readme = "README.md"
keywords = ["ai", "fleet", "agent", "rust"]
categories = ["science"]

[dependencies]
serde = { version = "1", features = ["derive"] }
